// 21: They think of something you could store in a TypeScript Object. Write a program that creates Objects
// containing these items.
let person:{
      name:string,
      age:number,
      gender:string,
      isMarried:boolean,
} = {
    name:"Ubaid",
    age:17,
    gender:"Male",
    isMarried:false
}
console.log(`Person Name: ${person.name}, Person Age: ${person.age}, Person Gender: ${person.gender}, Is Person
    is Married: ${person.isMarried} `);
